version: "3.8"

services:
  db:
    image: postgres:17.6
    container_name: postgres-fastone
    environment:
      POSTGRES_DB: fastone
      POSTGRES_USER: f1
      POSTGRES_PASSWORD: qwer1234
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: